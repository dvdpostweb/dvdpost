<% path = product_path(:id => streaming_product.to_param, :source => @wishlist_source[:vod_suggestion]) %>
<li>
  <div class="stars" id="rating-stars-<%= streaming_product.to_param %>" product_id ="<%= streaming_product.to_param %>">
    <%= render :partial => 'products/rating', :locals => {:product => streaming_product, :background => :white, :size => :small, :source => @wishlist_source[:vod_suggestion]} %>
  </div>
  <% link_to path, :class => ((streaming_product.streaming?(params[:kind], session[:country_id]) and streaming_access?) ? 'vod' : product_media_id(streaming_product.media)) do %>
    <%= product_image_tag streaming_product.image, :width => 76, :height => 118, :alt => "#{streaming_product.title} (#{streaming_product.year})" %>
  <% end %>
    <% token = current_customer.get_token(streaming_product.imdb_id) %>
    <%= render :partial => 'products/streaming', :locals => {:product => streaming_product, :token => token, :text => :short, :source => @wishlist_source[:vod_suggestion], :response_id => nil } %>
  <%= link_to streaming_product.title, path, :class => 'more' %>
</li>
