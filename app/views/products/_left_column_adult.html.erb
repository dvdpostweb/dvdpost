<!--   ==============   LEFT COLUMN   ==============   -->
<div id="left_column">
  <!-- test -->
  <% form_for :search_filter, filter, {:url => search_filters_path(:view_mode => params[:view_mode], :list_id => params[:list_id], :category_id => params[:category_id], :actor_id => params[:actor_id], :director_id => params[:director_id], :studio_id => params[:studio_id]), :html => {:id => 'search'}} do |f| %>
  <ul id="left-nav">
    <li id="catalogue_vod">
      <div id="catalogue_title_vod"><%= t('.streaming') %></div>
        <ul id="vod_list">
          <%= left_column_vod(params) %>
        </ul>
    </li>
    <li id="catalogue_dvd">
      <div id="catalogue_title_dvd"><%= t('products.left_column.all_medias') %></div>
      <ul id="dvd_list">
        <%= left_column(params) %>
      </ul>
    </li>
  </ul>
  <%= render :partial => 'products/index/filters_adult', :locals => {:countries => @countries, :filter => filter, :f => f} if params[:action] == 'index' %>
  <% end %>
    <div id="tag_area">
      <h2 class="miso"> Tags </h2>
      <div class="tags">
        <%= render :partial => "/products/index/tag", :collection => @collections %>								
  		</div>
    </div>
</div>

<!--   ==============   END LEFT COLUMN   ==============   -->
