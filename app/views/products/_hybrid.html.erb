<% load_color = load_color || :normal %>
<%= render :partial => 'products/wishlist', :locals => {:product => product, :text => text, :source => source, :load_color => load_color, :response_id => response_id} %>
<% if streaming_product %>
  <%= render :partial => 'products/streaming', :locals => {:product => product, :token => @tokens, :text => text, :source => source, :response_id => response_id } %>
<% end %>
