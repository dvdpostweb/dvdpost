<% path = get_path(theme) %>
<div id="week-theme">
	<h3><%= @theme_month ? t('.title') : t('.title_rand') %></h3>
	<%= link_to image_tag(theme.image_home_page, :size => "267x121"), path, :alt => t('.title') %>
	<%= link_to t('.see').upcase, themes_path(), :class => :osc %>
</div>
