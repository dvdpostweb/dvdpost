<% if advertising_product %>
<% data = advertising_product.description_data() %>
<% product_title = data[:title] %>
<% product_image = data[:image] %>
<li>
  <div class="stars" id="rating-stars-<%= advertising_product.to_param %>" product_id ="<%= advertising_product.to_param %>">
    <%= render :partial => 'products/rating', :locals => {:product => advertising_product, :background => :black, :size => :small} %>
  </div>
  <% link_to product_path(:id => advertising_product.to_param), :class => "cover #{product_media_id(advertising_product.media)}" do %>
    <%= product_image_tag product_image, :width => 56,:height=> 88, :alt => "#{product_title} (#{advertising_product.year})" %>
    <%= content_tag(:span, '', :class => 'new') if advertising_product.is_new? %>
  <% end %>
  <%= render :partial => 'products/wishlist', :locals => {:product => advertising_product, :text => :short, :source => @wishlist_source[id.to_sym], :load_color => :black} %>
  <%= link_to product_title, product_path(:id => advertising_product.to_param), :class => 'more'  %>
</li>
<% end %>