<div class="top-wrap" id="top-rents">
  <div class="ttl pie">Top des plus loués</div>
  <div class="top pie"> <span class="thumb"><%= image_tag products.first.image, :title => products.first.title, :width => 130, :height => 184 %></span>
    <ul>
      <% i = 0 %>
      <% products.collect do |product| %>
      <% i += 1 %>
      <% number = i <10 ? "0#{i}" : i %>
      <li>
        <a href="<%= product_path(:id => product.to_param) %>">
          <span class="number"><%= number %>.</span> <%= truncate(product.title, :length => 30) %>
        </a>
        <span class="rating" id="rating-stars-<%= product.to_param %>" product_id ="<%= product.to_param %>">
          <%= render :partial => 'products/rating', :locals => {:product => product, :background => :pink, :size => :big} %>
        </span>
        </li>
      <% end %>
    </ul>
  </div>
</div>