<% if token %>
  <% token_code = token.token %>
<% else %> 
  <% if Token.dvdpost_ip?(request.remote_ip) || (current_customer && current_customer.super_user?)%>
      <% Rails.logger.debug { "@@@" }%>
      <% token_code = DVDPost.generate_token_from_alpha(filename, params[:kind]) %>
  <% end %>
<% end %>

<%= flowplayer(filename, source, streaming, token_code, browser) %>