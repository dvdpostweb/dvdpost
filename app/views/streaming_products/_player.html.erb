<% if token %>
  <% token_code = token.token %>
<% else %> 
<% if Token.dvdpost_ip?(request.remote_ip) && source == StreamingProduct.source[:alphanetworks] %>
<% Rails.logger.debug { "@@@free" }%>
 <% token_code = DVDPost.generate_token_from_alpha(filename) %>
<% end %>

<% end %>
<%= flowplayer(filename, source, streaming, (token_code rescue 'dvdpost')) %>